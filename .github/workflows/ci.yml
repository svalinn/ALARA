name: ALARA Build/TEST

on:
    workflow_dispatch:
    pull_request:
    push:

jobs:
    Build:
        runs-on: ubuntu-latest

        steps:
        - name: Checkout repository
          uses: actions/checkout@v3

        - name: Build
          run: |
            mkdir bld
            cd bld
            cmake ../
            make -j 8


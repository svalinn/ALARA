\NeedsTeXFormat{LaTeX2e}[1994/06/01]
\ProvidesPackage{mythesis}
\RequirePackage{amsmath,amssymb,vmargin,overcite}
\RequirePackage{subfigure,epsfig,floatflt}
\RequirePackage{multirow,moreverb,array}
\RequirePackage{supertab,ulem,draftcopy}

\renewcommand{\baselinestretch}{1.66}
\setpapersize{USletter}
\setmarginsrb{1.25in}{1.25in}{1in}{1in}{0.5cm}{0.5cm}{0cm}{0.7cm}

%%% Float control
\renewcommand{\topfraction}{1.0}
\renewcommand{\bottomfraction}{1.0}
\renewcommand{\textfraction}{0.1}
\setlength{\floatsep}{0pt plus 2pt minus 2pt}
\setlength{\textfloatsep}{12pt plus 2pt minus 2pt}
\setlength{\intextsep}{12pt plus 2pt minus 2pt}

%% package subfigure
\renewcommand{\subfigtopskip}{5pt}
\renewcommand{\subfigbottomskip}{5pt}
\renewcommand{\subfigcapskip}{0pt}
\renewcommand{\subfigcapmargin}{5pt}

%%% Shortcut formatting defintions
\def\ALARA{\textsf{ALARA}}
\def\nary{\textsl{n}-ary }
\def\pc{\textsl{pc}}
\def\mat#1{\mbox{$\mathbf{#1}$}}

%% environtments to help describe Perl modules in appendix
\newenvironment{APIdescription}
               {\list{}{\labelwidth\z@ \itemindent-\leftmargin
                        \let\makelabel\APIdescriptionlabel}}
               {\endlist}
\newcommand*\APIdescriptionlabel[1]{\hspace\labelsep
                                \ttfamily\bfseries #1}
\newenvironment{dataStructure}
               {\list{}{\labelwidth\z@ \itemindent-\leftmargin
                        \let\makelabel\dataStructurelabel}}
               {\endlist}
\newcommand*\dataStructurelabel[1]{\hspace\labelsep
                                \ttfamily\bfseries \{#1\}}
\def\@listi{\leftmargin\leftmargini
            \parsep 5\p@  \@plus2.5\p@ \@minus\p@
            \topsep 5\p@ \@plus4\p@   \@minus6\p@
            \itemsep0\p@  \@plus2.5\p@ \@minus\p@}
\let\@listI\@listi
\@listi

%%% make the figure label bold and make single space
\long\def\@makecaption#1#2{%
  \def\baselinestretch{1}\normalsize
  \vskip\abovecaptionskip
  \sbox\@tempboxa{\textbf{#1:} #2}%
  \ifdim \wd\@tempboxa >\hsize
    \textbf{#1:} #2\par
  \else
    \global \@minipagefalse
    \hb@xt@\hsize{\hfil\box\@tempboxa\hfil}%
  \fi
  \vskip\belowcaptionskip}

\def\mycap#1{
  \def \@captype {#1}
   \refstepcounter\@captype
   \@dblarg{\@caption\@captype}
}


\newenvironment{myabstract}[2]{%
  \newpage
  \thispagestyle{empty}
  \selectlanguage{#1}
  \begin{center}
    \textbf{#2}\\
    \ \\
  \end{center}
  \noindent\textbf{\abstractname}
  \addcontentsline{toc}{section}{\abstractname}

  \renewcommand{\baselinestretch}{1.12}\normalsize}
{\renewcommand{\baselinestretch}{1.25}\normalsize}

  
\renewcommand{\thefootnote}{\fnsymbol{footnote}}


\renewcommand\appendix{\par
  \setcounter{chapter}{0}%
  \setcounter{section}{0}%
  \@openrightfalse % Appendix chapters don't need to openright
  \renewcommand\@chapapp{\appendixname}%
  \renewcommand\thechapter{\@Alph\c@chapter}}


\pagestyle{myheadings}

%% based on latex original definition of \hline
\def\thickerlinemult{6}
\def\thicklinemult{3}
\def\thline#1{%
  \noalign{\ifnum0=`}\fi\hrule \@height #1\arrayrulewidth \futurelet
   \reserved@a\@xhline}


\renewcommand{\thefootnote}{\alph{footnote}}

\newenvironment{binaryFile}
        {\setlength{\itemsep}{0pt \@plus 0pt \@minus 0pt}
         \setlength{\parsep}{0pt \@plus 0pt \@minus 0pt}
         \begin{itemize}}
        {\end{itemize}}

\allowdisplaybreaks
